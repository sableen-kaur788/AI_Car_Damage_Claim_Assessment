# Copy each KEY=value into Render → Web Service → Environment (no file upload).
# Replace placeholders. Never commit real secrets to Git.

DATABASE_URL=
JWT_SECRET=
GROQ_API_KEY=

# After your static site deploys, use its URL (comma-separated, no spaces unless quoted in Render)
CORS_ORIGINS=https://YOUR-FRONTEND-NAME.onrender.com,http://localhost:3000

# Leave empty to use default: <repo>/models/epoch80.pt
MODEL_PATH=

APP_ENV=production
ACCESS_TOKEN_EXPIRE_MINUTES=1440
REFRESH_TOKEN_EXPIRE_DAYS=7
RESET_TOKEN_EXPIRE_MINUTES=30
